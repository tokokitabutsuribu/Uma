# QR受付・スコア自動精算システム 操作マニュアル

## 全体の流れ

```
[事前準備]                                    [当日]
サーバーPCを用意                    運営：管理デバイス(admin.html)を操作
 └ npm install / npm start          参加者：読み取り端末(scan.html)でQRをスキャン
 └ 各端末にURLを共有                          ↓
                                    ラウンド開始 → 参加者が賭け → 受付終了(レート確定)
                                     → 結果入力 → 自動精算（スコア増減）→ 次のラウンドへ
```

---

## 1. 事前準備（イベント開始前）

1. サーバー役のPC（Node.js導入済み）に、プロジェクト一式をコピー
2. ターミナルで以下を実行

   ```bash
   cd qr-checkin
   npm install
   npm start
   ```

3. 起動ログに表示されるURLをメモする（例：`https://192.168.1.23:3000/`）

   ```
   読み取り端末用: https://192.168.1.23:3000/scan.html
   管理デバイス用: https://192.168.1.23:3000/admin.html
   ```

4. **読み取り端末（スタッフ・参加者用）**の数だけ、上記URLの `scan.html` を各端末のブラウザで開いておく
   - 初回アクセス時に「保護されていません」等の警告が出るので、「詳細」→「このまま進む」を選ぶ（自己署名証明書によるもので、正常な動作です。1端末につき最初の1回だけ）
5. **管理デバイス**（運営が持つ1台）で `admin.html` を開く（同様に初回のみ警告が出ます）
6. 本番前に、管理デバイスの「全データ初期化」ボタンでテストデータを消しておく

---

## 2. 管理デバイス（admin.html）の操作

### 起動時にやること

- 上部の「操作者名」欄に自分の名前を入力（スコアを手動編集した際に記録される）

### ラウンドの回し方（5ステップ）

「ラウンド」タブで操作します。

| ステップ | 操作 | 内容 |
|---|---|---|
| ① | 選択肢を入力し「ラウンドを開始する」 | 5つの選択肢（空欄なら1〜5の番号）を決めてラウンドを開く |
| ② | （参加者が入力するのを待つ） | 参加者は読み取り端末でIDをスキャンし、自分で賭け金・予想を入力する |
| ③ | 「受付終了してレートを確定する」 | これ以降、新しい投票は受け付けない。単勝・3連単それぞれのレート（倍率）が自動計算される |
| ④ | 1着・2着・3着を選んで「結果を確定して精算する」 | 実際の結果を入力すると、的中者に自動で配当が加算される。ラウンドはここで終了 |
| ⑤ | 次のラウンドへ | ①に戻って次のラウンドを開始できる。前のラウンドの参加登録は自動的にリセットされている |

**ポイント**
- レート＝そのラウンドの賭け金合計 ÷ その選択肢に賭けられた金額の合計
- 誰も的中しなかった場合、そのプールの配当はありません（追加の持ち出しは発生しません）
- 精算結果と過去のラウンド履歴は「ラウンド」タブの下部でいつでも確認できます

### その他のタブ

| タブ | できること |
|---|---|
| 参加者・スコア | 全参加者のID・現在スコア・スキャン回数を一覧表示。「編集」でスコアを直接書き換え可能 |
| スキャン履歴 | いつ・どの端末で・どのIDが読み取られたかの生ログ |
| スコア変更履歴 | 手動編集・賭け金の差し引き・配当のすべての増減記録（監査用） |

各タブとも、右上の「CSVで書き出し」でその時点の一覧をダウンロードできます。

### 注意が必要な操作

- **「全データ初期化」ボタン**：参加者・スコア・全履歴・ラウンドを完全に削除します。**本番前のテストのみ**使用し、運用中は絶対に押さないでください（2段階の確認ダイアログが出ます）。

---

## 3. 読み取り端末（scan.html）の操作

### 基本のスキャン

1. 端末名（例：受付A）を入力しておく
2. カメラの使用を許可する
3. 参加者のQRコード（ID）をカメラに映す → 自動で読み取り、ID・現在スコアが表示される

### ラウンド受付中の場合

QRを読み取ると、そのまま賭け入力画面が表示されます。参加者本人に以下を入力してもらってください。

1. **賭け方を選ぶ**：「単勝（1つ選択）」か「3連単（順列3つ）」
2. **予想を選ぶ**
   - 単勝：選択肢を1つタップ
   - 3連単：1着→2着→3着の順にタップ（同じボタンをもう一度押すと選択解除）
3. **賭け金を入力**：クイックボタン（10%／25%／50%／全額）か直接入力
4. 「この内容で確定する」を押すと送信完了。結果発表を待つ

- すでに投票済みのIDを再度スキャンすると、投票内容が確認表示されます（変更不可）
- ラウンドが受付中でない時間帯は、通常のID・スコア確認のみが表示されます

---

## 4. 2日間運用のチェックリスト

- [ ] サーバーPCは同じ機体を使う（`data/store.json` にデータが保存されるため、再起動しても記録は消えません）
- [ ] 1日目終了時に `data/store.json` を別の場所にコピーしてバックアップしておく
- [ ] サーバーPCのIPアドレスが変わらないか、2日目の朝に一度確認する（変わっていたら各端末に新しいURLを共有）
- [ ] ラウンドを開始したまま放置しない（受付終了・結果入力・精算まで一連の流れを完了させる）

---

## 5. トラブルシューティング

| 症状 | 対処 |
|---|---|
| 読み取り端末でカメラが映らない | ブラウザのカメラ許可設定を確認。自己署名証明書のため、初回アクセス時に出る警告画面で「詳細」→「このまま進む」を選ぶ必要がある（1端末につき1回） |
| コンソールに「Script error.」と出る | ライブラリを自己ホスト化済みのため通常は発生しません。発生する場合はサーバーが起動しているか、URLが正しいか確認してください |
| 端末から管理画面につながらない | サーバーPCのIPアドレスが変わっていないか確認。同じWi-Fiに接続しているかも確認 |
| 賭け金を入力できない（送信ボタンが押せない） | 賭け金がスコアを超えていないか、予想（単勝1つ／3連単3つ）を選び終えているか確認 |
| ラウンドを開始しようとすると「既に進行中のラウンドがあります」と出る | 前のラウンドをまだ精算していません。「受付終了」→結果入力→精算まで完了させてください |

---

## 6. よく使うボタン早見表（管理デバイス）

- **ラウンドを開始する** … 新しい予想ゲームを始める
- **受付終了してレートを確定する** … 投票を締め切り、倍率を計算する
- **結果を確定して精算する** … 実際の結果を反映してスコアを自動増減する
- **編集**（参加者・スコアタブ） … 特定のIDのスコアを手動で書き換える
- **CSVで書き出し** … その時点の一覧をファイルとして保存する
- **全データ初期化（テスト用）** … 全記録を削除して最初からやり直す（本番中は使用しない）
